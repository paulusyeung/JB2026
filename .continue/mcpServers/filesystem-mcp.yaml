name: Filesystem MCP Server
version: 0.0.1
schema: v1
mcpServers:
  - name: filesystem
    command: "/home/paulus/.nvm/versions/node/v24.14.1/bin/node"
    args:
      - "/home/paulus/.nvm/versions/node/v24.14.1/lib/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js"
      - "/home/paulus/Projects/JB2026"
    env:
      # Manually provide the path to your Node binaries and standard system tools
      PATH: "/home/paulus/.nvm/versions/node/v24.14.1/bin:/usr/local/bin:/usr/bin:/bin"
      # Fixes the common "Module not found" crash
      NODE_PATH: "/home/paulus/.nvm/versions/node/v24.14.1/lib/node_modules"
      # Suppress any non-critical logs that crash the connection
      NODE_NO_WARNINGS: "1"
    connectionTimeout: 60000
